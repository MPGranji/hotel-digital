{
  "name": "hotel-digital",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@hotel-digital/web",
    "build": "npm run build --workspace=@hotel-digital/web",
    "lint": "npm run lint --workspace=@hotel-digital/web",
    "typecheck": "npm run typecheck --workspace=@hotel-digital/web",
    "check": "npm run lint && npm run typecheck && npm run build"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
